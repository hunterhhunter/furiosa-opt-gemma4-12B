# Reproduction document template

The generated document creates a separate worktree, checks and applies baseline
and candidate patches in order, regenerates the exact kernel schedule, and lists
commands for recording tool versions.
